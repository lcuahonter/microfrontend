# Expose the deployment using a Service
apiVersion: v1
kind: Service
metadata:
  name: modulefederation-usuarios-microfront-service-dev
spec:
  selector:
    app: modulefederation-usuarios-microfront-dev
  ports:
    - protocol: TCP
      port: 4230
      targetPort: 80
  type: NodePort
