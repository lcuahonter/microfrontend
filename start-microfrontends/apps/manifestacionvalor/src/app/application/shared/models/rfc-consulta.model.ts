/**
 * Interfaz que define los campos del rfc de consulta.
 * @interface RfcConsulta
 */
export interface RfcConsulta {
    rfc: string;
    nombreRazonSocial: string;
    tipoFigura: string;
    tipoFiguraDescripcion: string;
}