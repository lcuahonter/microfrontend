import { Component } from '@angular/core';

/**
 * Componente para mostrar y gestionar el monto y factor de ampliación.
 * @export
 * @component
 */
@Component({
  selector: 'app-monto-factor',
  templateUrl: './monto-factor.component.html',
  styleUrl: './monto-factor.component.scss',
  standalone: true
})

export class MontoFactorComponent {
  // Este componente actualmente no contiene lógica interna.
}
