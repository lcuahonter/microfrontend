services:
#----------------------------------------------------------------
  modulefederation-manifestacionvalor-microfront:

    image: ghcr.io/vucem30/dockerized-modulefederation-manifestacionvalor-microfront:1.0.0

    build:
      context: ../../
      dockerfile: apps/manifestacionvalor/Dockerfile.qa
      args:
        - NODE_ENV=production
    container_name: modulefederation-manifestacionvalor-microfront
    restart: unless-stopped
    expose:
      - '4234'
    ports:
      - "4234:80"
    networks:
      - modulefederation-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    environment:
      - NODE_ENV=production
    volumes:
      - nginx_logs:/var/log/nginx
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
#----------------------------------------------------------------
networks:
  modulefederation-network:
    driver: bridge

volumes:
  nginx_logs:
